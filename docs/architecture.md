# Architecture

The platform separates domain objects, preprocessing, features, models, uncertainty, explainability, visualization, APIs, and applications. Model artifacts should be versioned and linked to datasets, configs, metrics, and validation reports.
