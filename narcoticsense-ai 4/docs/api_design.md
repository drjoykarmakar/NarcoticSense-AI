# API Design

The public API exposes health checks, spectrum upload, preprocessing, training, prediction, uncertainty, explanation, reporting, and assistant endpoints. All model outputs should include model version, calibration status, confidence, and responsible-use warnings.
